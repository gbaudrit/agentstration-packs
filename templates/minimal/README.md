# Minimal Pack template

Copy this directory under the appropriate `packs/<purpose>/<audience>/<name>` collection, replace placeholders, add README and CHANGELOG, then run the repository validation. Reference the declared `agent-model` binding from each Agent with `modelProfile: { binding: agent-model }`.
